export type userType = {
    id: string; 
    name: string; 
    email: string; 
    created_at: string;
}

export type snackbarType = {
    open: boolean,
    vertical: string,
    horizontal: string,
    message: string
  }
  